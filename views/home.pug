extends layouts/main.pug
include mixins/videoBlock

block content
    .videos
        // each ~~ in controller에서 전달한 이름과 같아야함
        each item in videos
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.videoFile
            })